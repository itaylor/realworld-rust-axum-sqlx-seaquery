# Database Configuration
# Format: postgres://username:password@host:port/database_name
DATABASE_URL=postgres://realworld:password@localhost:5432/realworld

DATABASE_USER=realworld
DATABASE_PASSWORD=password

# Maximum number of connections in the database pool (default: 5)
DATABASE_MAX_CONNECTIONS=5

# HTTP Server Configuration
# HTTP_HOST=0.0.0.0
# HTTP_PORT=8080

# Security Configuration
# Password pepper - a secret key used for password hashing
# WARNING: This must remain constant! If you lose or change this value,
# all existing passwords will become unrecoverable!
# Generate a strong random value (e.g., using: openssl rand -base64 32)
PASSWORD_PEPPER=change_this_to_a_random_secret_value_and_keep_it_safe
